<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <EditTextPreference
        android:key="pref_save_path_key"
        android:title="@string/pref_save_path"
        android:summary="@string/pref_save_path_description"
        android:inputType="text"
        android:defaultValue="@string/pref_save_path_default"
        android:singleLine="true"/>

    <!-- <ListPreference
        android:key="pref_save_format_key"
        android:title="@string/pref_save_format"
        android:defaultValue="1"
        android:entries="@array/img_formats"
        android:entryValues="@array/img_formats_values"/> -->
</PreferenceScreen>